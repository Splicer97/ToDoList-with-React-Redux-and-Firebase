import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles ({
    root: {
        color: "#e8e8e8",
    },
    width: {
        marginTop: "200",
    },
    box: {
        marginLeft: "10",
    },
})

export default useStyles