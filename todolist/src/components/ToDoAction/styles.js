import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        textTransform: "none",
        color: "#c5c5c5",

    },
    textStyle: {
        color: "#c5c5c5",
    },
    visible: {
        visibility: 'visible',
    },
    hidden: {
        visibility: 'hidden',
    }
})

export default useStyles