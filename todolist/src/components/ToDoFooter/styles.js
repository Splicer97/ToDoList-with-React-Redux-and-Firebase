import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles ({
    root: {
        textTransform: "none",
        color: "#c5c5c5",

    },
    textstyle: {
        color: "#c5c5c5",
    }
})

export default useStyles