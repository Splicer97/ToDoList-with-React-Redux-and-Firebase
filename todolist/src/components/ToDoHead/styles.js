import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles ({
    qwerty: {
        color: "#e3d2d1",
        marginBottom: "30px",
        fontWeight: "100",
        fontSize: "9rem",
    }
})

export default useStyles